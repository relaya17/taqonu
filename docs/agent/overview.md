# Agent

Layered orchestrator: Planner → Context Builder → Memory Retriever → Knowledge Retriever → Tool Router → Policy Engine → Executor → Verifier.

Read vs Write modes are enforced in `@atlas/agent-core`. Write actions require approval.
