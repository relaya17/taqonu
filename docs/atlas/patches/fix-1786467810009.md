# Patch proposal — Fix

Add a safe comment marker for smoke test

## Graph
```
Repository
├── Apps (api, web, worker)
├── Packages (agent-core, code-intelligence, config, database, embeddings, experts, integrations, knowledge, observability, qa-core, shared, state)
├── Tests / CI / Infra (scan deeper via impact)
└── Integrations
```

_Generated by Atlas Code Intelligence. Apply is approval-gated._