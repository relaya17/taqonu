# Optional shared non-secrets only.
# Server secrets → apps/api/.env  (see apps/api/.env.example)
# Web public vars → apps/web/.env.local  (see apps/web/.env.example)

NODE_ENV=development
APP_NAME=ArletOS
PRODUCT_CODENAME=Atlas
LOG_LEVEL=info
