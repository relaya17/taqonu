# Golden BrokerOS fixture (Atlas 1.1 Proof)

In-repo stand-in when `ATLAS_GOLDEN_PROJECT_ROOT` / real `brokerOS-main` is missing.

Contains keyword signals for eval tasks **A–F** (optimistic lock, commission waterfall,
duplicate detection, DEF blockers). Lab-only — not a product lock.