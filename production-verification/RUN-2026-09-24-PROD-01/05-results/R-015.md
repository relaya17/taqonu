# R-015 — Production Persistence / Recovery

R-ID: R-015
Name: Production Persistence / Recovery
Environment: Production (Vercel) — taqonu-api.vercel.app, taqonu-control-plane.vercel.app
Commit: local HEAD 261c360d8fd7dc7e88e4fc8f9143d8225661a46c; deployed production build commit NOT ESTABLISHABLE (see R-001 Finding 2)
Execution timestamp: 2026-09-24T07:57:21Z – 2026-09-24T08:08:10Z (UTC)

## Expected behavior

State survives an approved production restart: state written before the restart is present
and correct after it, and no governance or audit data is lost.

## Actual behavior

The scenario could not be executed:

- The pre-restart write requires the API, which returns 503 on every route, so no state could
  be written to compare against.
- The production surfaces are Vercel serverless deployments. A "controlled restart" here means
  a redeploy or an infrastructure change to the production project. No such change was
  authorised for this run, and plan §10 requires stopping rather than improvising when an
  unknown infrastructure modification would be needed.
- Plan §R-015 permits execution only if the production environment permits a controlled
  restart, and directs marking BLOCKED otherwise. That condition does not hold.

No restart, redeploy or infrastructure change was performed. No production data was written,
migrated or deleted.

## Evidence files

```
00-baseline/service-health.txt
00-baseline/production-version.txt
04-evidence/api-503-config-error-raw.json
05-results/R-001.md
```

## Audit/Event identifiers

None. No pre-restart or post-restart state record exists for this run.

## Result

BLOCKED

## Reason

external blocker:
  (a) Production API unavailable — 503 CONFIG_ERROR on every route, so no pre-restart state
      could be written. Recorded once as FAILED at R-001, not re-counted here.
  (b) No authorisation exists to restart, redeploy or otherwise modify the production
      infrastructure; plan §R-015 directs BLOCKED rather than improvising an alternative, and
      §10 requires stopping when an unknown infrastructure modification would be required.

required condition:
  (a) A production API deployment that boots.
  (b) Explicit authorisation for one controlled production restart or redeploy, with an
      agreed maintenance window.
  (c) A production Control Plane operator or owner token to read state before and after.
  (d) A deployment-to-commit binding, so pre- and post-restart builds are comparable.

attempted action:
  - GET https://taqonu-api.vercel.app/health → 503
  - No restart, redeploy or infrastructure change was attempted — deliberately, per plan
    §R-015 and §10
  - Deployment identity re-checked for a restart baseline; none available
    (00-baseline/production-version.txt)

evidence:
  00-baseline/service-health.txt
  00-baseline/production-version.txt
  04-evidence/api-503-config-error-raw.json

Per plan §6 Case A this is not a failure of the Atlas persistence or recovery implementation.
