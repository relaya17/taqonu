# R-009 — Evidence Chain

R-ID: R-009
Name: Evidence Chain
Environment: Production (Vercel) — taqonu-api.vercel.app, taqonu-control-plane.vercel.app
Commit: local HEAD 261c360d8fd7dc7e88e4fc8f9143d8225661a46c; deployed production build commit NOT ESTABLISHABLE (see R-001 Finding 2)
Execution timestamp: 2026-09-24T07:57:21Z – 2026-09-24T08:08:10Z (UTC)

## Expected behavior

The full chain Policy → Event → Audit Record → Evidence Reference is traceable in production
for one real action, with each link resolvable from the previous one.

## Actual behavior

The scenario could not be executed. No link in the chain was observable:

- Policy: `GET /api/v1/policies` → 401.
- Event: no production action could be produced; the API returns 503 on every route.
- Audit record: `GET /api/v1/audit` → 401.
- Chain integrity: `GET /api/v1/audit/verify` → 401, so even the standing chain-verification
  result could not be read.

No chain link was traversed. The local hash-chained `.atlas/audit/audit.ndjson` was not
modified and was not offered as a substitute for production evidence.

## Evidence files

```
00-baseline/service-health.txt
02-observations/control-plane-readonly-probes.txt
04-evidence/api-503-config-error-raw.json
05-results/R-001.md
```

## Audit/Event identifiers

None. No production event, audit record or evidence reference was obtained.

## Result

BLOCKED

## Reason

external blocker:
  (a) Production API unavailable — 503 CONFIG_ERROR on every route. Recorded once as FAILED
      at R-001, not re-counted here.
  (b) Control Plane policy, audit and audit-verify endpoints require
      `ATLAS_CONTROL_PLANE_TOKEN`, not held by this operator.

required condition:
  (a) A production API deployment that boots.
  (b) A production Control Plane operator or owner token with audit and policy read access.
  (c) A deployment-to-commit binding.

attempted action:
  - GET https://taqonu-control-plane.vercel.app/api/v1/policies → 401
  - GET https://taqonu-control-plane.vercel.app/api/v1/audit → 401
  - GET https://taqonu-control-plane.vercel.app/api/v1/audit/verify → 401
  - GET https://taqonu-api.vercel.app/health → 503

evidence:
  00-baseline/service-health.txt
  04-evidence/api-503-config-error-raw.json
  02-observations/control-plane-readonly-probes.txt

Per plan §6 Case A this is not a failure of the evidence-chain implementation.
