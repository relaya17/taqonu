# R-005 — Dispatch Guard

R-ID: R-005
Name: Dispatch Guard
Environment: Production (Vercel) — taqonu-api.vercel.app, taqonu-control-plane.vercel.app
Commit: local HEAD 261c360d8fd7dc7e88e4fc8f9143d8225661a46c; deployed production build commit NOT ESTABLISHABLE (see R-001 Finding 2)
Execution timestamp: 2026-09-24T07:57:21Z – 2026-09-24T08:08:10Z (UTC)

## Expected behavior

The existing `dispatchAgentAction` guard allows or blocks a controlled dispatch exactly
according to the configured production policy.

## Actual behavior

The scenario could not be executed:

- `dispatchAgentAction` is reached only through the API request path. Every route on
  `https://taqonu-api.vercel.app` returns 503 before routing, so the guard is never invoked.
- Policy read-back: `GET /api/v1/policies` returns 401 `Control Plane authentication required`,
  so the applicable production policy could not even be read.

No dispatch was attempted. **No policy was weakened to force execution** (plan §1.2, §R-005).

## Evidence files

```
00-baseline/service-health.txt
02-observations/control-plane-readonly-probes.txt
04-evidence/api-503-config-error-raw.json
05-results/R-001.md
```

## Audit/Event identifiers

None. Audit retrieval returned 401 without a production Control Plane token.

## Result

BLOCKED

## Reason

external blocker:
  (a) Production API unavailable — 503 CONFIG_ERROR on every route. Recorded once as FAILED
      at R-001, not re-counted here.
  (b) Control Plane policy endpoints require `ATLAS_CONTROL_PLANE_TOKEN`, not held by this
      operator.

required condition:
  (a) A production API deployment that boots.
  (b) A production Control Plane operator or owner token.
  (c) A deployment-to-commit binding.

attempted action:
  - GET https://taqonu-api.vercel.app/health → 503 (no route on this host is reachable)
  - GET https://taqonu-control-plane.vercel.app/api/v1/policies → 401

evidence:
  00-baseline/service-health.txt
  04-evidence/api-503-config-error-raw.json
  02-observations/control-plane-readonly-probes.txt

Per plan §6 Case A this is not a failure of the dispatch guard implementation. The guard was
never reached, so nothing is known about its production behavior in either direction.
