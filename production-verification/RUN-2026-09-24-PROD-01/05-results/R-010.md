# R-010 — Epistemic State

R-ID: R-010
Name: Epistemic State
Environment: Production (Vercel) — taqonu-api.vercel.app, taqonu-control-plane.vercel.app
Commit: local HEAD 261c360d8fd7dc7e88e4fc8f9143d8225661a46c; deployed production build commit NOT ESTABLISHABLE (see R-001 Finding 2)
Execution timestamp: 2026-09-24T07:57:21Z – 2026-09-24T08:08:10Z (UTC)

## Expected behavior

An actual production result is assigned the correct epistemic state (VERIFIED / OBSERVED /
INFERRED / UNVERIFIED / CONFLICTED / INSUFFICIENT_EVIDENCE) for the evidence that actually
exists behind it, and that assignment is observable in production.

## Actual behavior

The scenario could not be executed. No production result was produced to be classified:

- The API returns 503 on every route, so no result with an epistemic state was generated.
- `GET /api/v1/self-audit` and `GET /api/v1/governance/decisions`, which would expose
  production epistemic classifications, both return 401 `Control Plane authentication
  required`.

No epistemic state was read from production.

Note on scope: the epistemic discipline is nevertheless applied to *this run* — every item in
this bundle is classified by the evidence actually captured, and no item is upgraded on the
strength of source code or prior test results (plan §1.3). That is the run methodology, not
evidence about the production epistemic-state feature, and it is not counted as such.

## Evidence files

```
00-baseline/service-health.txt
02-observations/control-plane-readonly-probes.txt
04-evidence/api-503-config-error-raw.json
05-results/R-001.md
```

## Audit/Event identifiers

None. No production result or classification record was retrieved.

## Result

BLOCKED

## Reason

external blocker:
  (a) Production API unavailable — 503 CONFIG_ERROR on every route, so no classifiable result
      exists. Recorded once as FAILED at R-001, not re-counted here.
  (b) Control Plane self-audit and governance endpoints require `ATLAS_CONTROL_PLANE_TOKEN`,
      not held by this operator.

required condition:
  (a) A production API deployment that boots.
  (b) A production Control Plane operator or owner token.
  (c) A deployment-to-commit binding.

attempted action:
  - GET https://taqonu-control-plane.vercel.app/api/v1/self-audit → 401
  - GET https://taqonu-control-plane.vercel.app/api/v1/governance/decisions → 401
  - GET https://taqonu-api.vercel.app/health → 503

evidence:
  00-baseline/service-health.txt
  04-evidence/api-503-config-error-raw.json
  02-observations/control-plane-readonly-probes.txt

Per plan §6 Case A this is not a failure of the epistemic-state implementation.
