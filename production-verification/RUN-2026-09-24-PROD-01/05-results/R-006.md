# R-006 — Risk Policy

R-ID: R-006
Name: Risk Policy
Environment: Production (Vercel) — taqonu-api.vercel.app, taqonu-control-plane.vercel.app
Commit: local HEAD 261c360d8fd7dc7e88e4fc8f9143d8225661a46c; deployed production build commit NOT ESTABLISHABLE (see R-001 Finding 2)
Execution timestamp: 2026-09-24T07:57:21Z – 2026-09-24T08:08:10Z (UTC)

## Expected behavior

A controlled action of a known risk category is classified and enforced according to the
configured production risk policy, recording action, risk classification, policy, decision,
execution/block result and audit record.

## Actual behavior

The scenario could not be executed:

- Risk classification happens inside the API request path; every route on
  `https://taqonu-api.vercel.app` returns 503 before routing.
- Policy definitions: `GET /api/v1/policies` and `GET /api/v1/policies/:entityType/:action`
  both return 401 `Control Plane authentication required`.

No action was dispatched and no risk classification was observed. No policy was modified.

## Evidence files

```
00-baseline/service-health.txt
02-observations/control-plane-readonly-probes.txt
04-evidence/api-503-config-error-raw.json
05-results/R-001.md
```

## Audit/Event identifiers

None. Audit retrieval returned 401 without a production Control Plane token.

## Result

BLOCKED

## Reason

external blocker:
  (a) Production API unavailable — 503 CONFIG_ERROR on every route. Recorded once as FAILED
      at R-001, not re-counted here.
  (b) Control Plane policy endpoints require `ATLAS_CONTROL_PLANE_TOKEN`, not held by this
      operator.

required condition:
  (a) A production API deployment that boots.
  (b) A production Control Plane operator or owner token.
  (c) A deployment-to-commit binding.

attempted action:
  - GET https://taqonu-control-plane.vercel.app/api/v1/policies → 401
  - GET https://taqonu-api.vercel.app/health → 503

evidence:
  00-baseline/service-health.txt
  04-evidence/api-503-config-error-raw.json
  02-observations/control-plane-readonly-probes.txt

Per plan §6 Case A this is not a failure of the Atlas risk-policy implementation.
