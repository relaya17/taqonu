# R-001 — Production Baseline (remediation pass)

R-ID: R-001
Name: Production Baseline — node-pty deployment artifact remediation
Environment: Production (Vercel) — https://taqonu-api.vercel.app ; local build host (Windows, Node v22.23.2)
Commit: 261c360d8fd7dc7e88e4fc8f9143d8225661a46c PLUS one uncommitted change to apps/api/scripts/bundle-vercel.mjs. Deployed Production build: NOT ESTABLISHED — the corrected artifact was not deployed.
Execution timestamp: 2026-09-24T08:45:35Z – 2026-09-24T08:59:40Z (UTC)

## Expected behavior

1. The esbuild configuration treats `node-pty` as an external, so its native
   binding is resolved by the installed package rather than sought relative to
   the emitted bundle.
2. The API build and the Vercel bundle build both complete with no new errors.
3. The corrected artifact is deployed to Production.
4. `GET https://taqonu-api.vercel.app/health` returns HTTP 200.
5. `Cannot find module './prebuilds/linux-x64//pty.node'` no longer occurs during
   API initialization in Production.

## Actual behavior

### Steps 1 and 2 — completed and evidenced

`apps/api/scripts/bundle-vercel.mjs:30` was changed from

    external: ["sharp", "pg-native", "@biomejs/biome", "fsevents"],

to

    external: ["sharp", "pg-native", "@biomejs/biome", "fsevents", "node-pty"],

That is the entire source change: one line, one insertion, one deletion, in the
one authorized file.

  - `pnpm --filter "@atlas/api..." run build` → exit 0. 23 workspace projects
    compiled with `tsc`. No TypeScript error.
  - `pnpm --filter @atlas/api run bundle:vercel` → exit 0. Bundle written,
    7500876 bytes (was 7522799 bytes with the pre-fix external list).
  - One esbuild warning (`import.meta` under `cjs`,
    `dist/services/repo-root.js:18`). An A/B control build with the pre-fix
    external list emits the identical warning, so it is pre-existing and was not
    introduced here.
  - Bundle content, post-fix vs. pre-fix control:
    `Failed to load native module` 0 vs 1 · `prebuilds/` 0 vs 1 ·
    `require("node-pty")` 1 vs 0.
  - Requiring the corrected bundle exactly as `apps/api/api/index.js:34` does
    succeeded: `BUNDLE_REQUIRE: OK`, `createRequestHandler` exported,
    `require("node-pty")` resolved. This is the precise phase that throws in
    Production. It is local evidence only.

### Step 3 — not performed

The corrected artifact was not deployed. Both available paths are closed to this
operator:

  - **Vercel Git auto-deploy**, the documented mechanism for this repository
    (`.github/workflows/ci.yml:3-4`: "This workflow does not control Vercel Git
    auto-deploy"). There is no deploy job in `.github/workflows/` and no deploy
    script in `package.json`. Triggering it requires a commit pushed to
    `origin/main`. Task section 13 states: "Do not commit. Do not push." No commit
    and no push were made.
  - **Vercel CLI direct deploy**. `vercel deploy --prod --yes` was attempted from
    `apps/api` with no token supplied and no token invented. It exited 1:
    `Error: No existing credentials found.` There is no `.vercel` project linkage
    anywhere in the repository and no `VERCEL_TOKEN`, `VERCEL_ORG_ID` or
    `VERCEL_PROJECT_ID` in the environment.

No development credential was substituted for a Production one.

### Steps 4 and 5 — unchanged, because step 3 did not occur

Re-probed at 2026-09-24T08:58:55Z with no credentials:

    HTTP/1.1 503 Service Unavailable
    {"error":{"code":"CONFIG_ERROR","message":"Failed to load native module:
    pty.node, checked: build/Release, build/Debug, prebuilds/linux-x64: Error:
    Cannot find module './prebuilds/linux-x64//pty.node'\nRequire stack:\n-
    /var/task/apps/api/vercel-bundle.cjs\n- /var/task/apps/api/api/index.js", ...}}

`/health`, `/api/v1/status`, `/` and `/api/v1/agents` all return 503. Production is
serving the previous, defective build. The contract violation recorded in
RUN-2026-09-24-PROD-01 is therefore still live and unremediated in Production.

### Additional finding — reported, not repaired

Booting the corrected bundle locally through `scripts/vercel-bundle-smoke.mjs`
did not reach a response. It failed at Fastify plugin load with
`TypeError: Dtr.Server is not a constructor` — a `ws` WebSocket Server interop
failure, unrelated to node-pty. An A/B control confirmed the pre-fix bundle fails
identically (`TypeError: trr.Server is not a constructor`), so this defect is
pre-existing and was not introduced by this change. It occurs later in boot than
the node-pty failure, so Production has not yet reached it. Once the node-pty
artifact defect is corrected in Production, this is the next boot defect
Production is expected to encounter.

Per task section 9, no speculative additional fix was made for it.

## Evidence files

```
00-baseline/git-baseline.txt
01-fix/bundle-vercel-diff.txt
02-build/build-output.txt
03-deployment/deployment-result.txt
03-deployment/deployment-identity.txt
04-evidence/api-baseline-raw.txt
04-evidence/health-raw.txt
04-evidence/health-raw-body.json
04-evidence/node-pty-post-fix.txt
04-evidence/node-pty-post-fix-local-smoke.txt
```

## Audit/Event identifiers

None. No Production deployment, action or audit record was created by this run.

## Result

BLOCKED

## Reason

Per task section 10, BLOCKED is recorded for the external credential requirement
only. It does not mean the Production defect is resolved — Production is still
returning 503 with the original `pty.node` error.

missing production secret:
  A Vercel Production deployment credential for this project. Any one of:
  a Vercel account session for the CLI (`vercel login`), a Vercel access token
  (`--token` / `VERCEL_TOKEN`) with deploy rights on the `taqonu-api` project, or
  authorization to push to `origin/main` so the existing Vercel Git auto-deploy
  is triggered. No value is recorded anywhere in this bundle.

where it is required:
  Task section 5, the deployment step. Concretely: `vercel deploy --prod` executed
  from `apps/api`, or a commit pushed to `origin/main` which triggers Vercel Git
  auto-deploy as described in `.github/workflows/ci.yml:3-4`.

which verification step depends on it:
  Every remaining step of R-001. Task section 8 requires all six of: corrected
  source deployed, Production build identity known, API initializes successfully,
  `/health` returns 200, the `pty.node` error gone, evidence bundle complete.
  Items 1 through 5 all depend on a Production deployment occurring. Only item 6
  is satisfied.

  R-002 through R-016 remain blocked behind this as well, and were not attempted
  in this pass, per task section 11.

attempted action:
  - `vercel deploy --prod --yes` from apps/api, no token supplied → exit 1,
    "No existing credentials found"
  - `vercel whoami` → "No existing credentials found"
  - Searched for project linkage and deployment automation: no `.vercel`
    directory in the repository, no deploy job in `.github/workflows/`, no deploy
    script in `package.json`
  - `GET https://taqonu-api.vercel.app/health` → 503, unchanged
  - Git auto-deploy deliberately NOT triggered: it requires a commit and push,
    which task section 13 prohibits

evidence:
  03-deployment/deployment-result.txt
  03-deployment/deployment-identity.txt
  04-evidence/health-raw.txt
  04-evidence/api-baseline-raw.txt

Note on classification. The fix itself is not blocked and is not speculative — it
is implemented, built, and evidenced by an A/B control. What is blocked is
deployment, and therefore Production verification. No part of this result claims
Production behaviour on the strength of local execution.
